# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Structure

This is a new project with Claude Code extension points configured:

- `.claude/commands/` - Custom slash commands (markdown files become `/command-name`)
- `.claude/agents/` - Custom agents for specialized tasks
- `.claude/docs/` - Documentation files for reference
